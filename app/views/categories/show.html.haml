- @body_class = 'scaffold scaffold-show'

%p#notice= notice

%p
  %b Name:
  = @category.name

%ul
  - @sites.each do |site|
    %li
      = link_to_site_id site.id

= link_to 'Edit', edit_category_path(@category), {class: 'btn btn-white'}
\|
= link_to 'Back', categories_path, {class: 'btn btn-white'}
